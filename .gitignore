__pycache__/
*.pyc
data/optimized_params/
data/
tananyag/
.venv/
venv/
config.json
trades.csv
# Jelszót tartalmazó config-másolatok — sose kerüljenek fel
config*másolata*.json
config*copy*.json
config.local.json

# A repoban TAROLT leforditott MQL binarisok (verziozva, jegyzekkel).
# Ezek SZANDEKOSAN a repoban vannak — lasd core/mt_deploy.py.
!mt4/compiled/
!mt5/compiled/

# Kezi laborator forgatokonyvei (tools/lab_scenario.py) — sajat kiserletek,
# nem a program resze. A `--minta` barmikor ujra eloallitja.
fk.json
sajat.json
lab_*.json

# IDE-beallitasok (PyCharm/IntelliJ) — gep- es felhasznalo-fuggo, nem a projekte.
.idea/

# Rust natív gyorsító mag — a FORRÁS a repóban, a lefordított bináris nem
# (gépfüggő, és `python tools/build_native.py` bármikor előállítja).
rust/tfbt/target/
*.dll
