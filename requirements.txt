# TradeForge futásidejű és teszt-függőségei.
#
# A projekt SZÁNDÉKOSAN Windows-specifikus: a MetaTrader 5 terminál csak
# Windowson érhető el, ezért a `MetaTrader5` csomagnak sincs Linux/macOS
# kiadása. A CI is emiatt fut `windows-latest` futtatón.
#
# Telepítés:  python -m pip install -r requirements.txt

# --- Alap -------------------------------------------------------------------
MetaTrader5>=5.0.45      # kommunikáció az MT5 terminállal (Windows-only)
numpy>=2.0
pandas>=2.2
optuna>=4.0              # paraméter-optimalizálás (study SQLite-ban)
pyarrow>=15.0            # historikus adatok Parquet-formátuma
fastparquet>=2024.2.0

# --- Az ml_ai stratégiához --------------------------------------------------
scikit-learn>=1.5
lightgbm>=4.0

# --- Egyéb modulok által használt -------------------------------------------
cryptography>=42.0       # licencfájl aláírás-ellenőrzése
requests>=2.31
matplotlib>=3.8          # a backtest-dialógus diagramjai
pillow>=10.0             # felület-képernyőkép (tools/ui_preview.py)
rich>=13.0               # a konzolos táblázatos nézet (`main.py console --tui`)
#   ⚠ NEM kötelező a futáshoz: enélkül a `console` parancssoros módja
#   változatlanul megy, és a program megmondja, mit kell telepíteni. Mérve
#   (2026-08-31): +6,6 MB RSS, 15 MB lemezen — a tkinter-felület importjainak
#   negyede, ezért fér bele egy 2 GB-os VM-be is.

# A kezi laboratorium (python main.py lab) Qt-s chart-felulete.
# ⚠ CSAK A LABORHOZ kell: a motor, a dashboard es a backtest ezek nelkul is fut.
# A matplotlib-rol valo valtas oka merve van (a lab_qt.py fejleceben): a
# rajzolas 5-15x gyorsabb, es a huzhato vonal / talalat-teszt / tengely-felirat
# keszen van benne — ezeket a matplotlib alatt kezzel irtuk, es sorra hibasak
# voltak. A PySide6 LGPL (nem GPL), tehat a licencelest nem szukiti.
pyqtgraph>=0.13
PySide6>=6.6
